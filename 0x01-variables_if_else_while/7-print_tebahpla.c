#include <stdio.h>
/**
 * main - entry point
 * Return: 0 if false, otherwise if otherwise.
 */
int main(void)
{
	char a = 123;
	char b = '\n';
while (a > 97)
{
	a--;
	putchar(a);
}
	putchar(b);
	return (0);
}
