#include <stdio.h>
/**
 * main - entry point
 * Return:0 if false, otherwise if otherwise.
 */
int main(void)
{
	char a = 64;
	char aa = 96;
	char b = '\n';
while (a < 90)
	a++;
		putchar(a);
while (aa < 122)
	aa++;
		putchar(aa);
		putchar(b);
	return (0);
}
