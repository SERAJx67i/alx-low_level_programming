#include <stdio.h>
/**
 * main - entry point
 * Return:0 if false, otherwise if otherwise.
 */
int main(void)
{
	int a = 47;
	int b = 10;
	int c = 44;
	int s = 32;
while (a < 57)
{
	a++;
	putchar(a);
	putchar(c);
	putchar(s);
}
	putchar(b);
	return (0);
}
