#include <stdio.h>
/**
 * main - entry point 
 * Return: 0 if false, otherwise if otherwise.
 */
int main(void)
{
	char a = 96;
	char b = '\n';
	char c = 100;
while (a < 101)
{
	a++;
		putchar(a);
}
while (c > 112)
	c++;
{
	putchar(c);
}
	return (0);
}
