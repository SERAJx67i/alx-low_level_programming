#include <stdio.h>
/**
 * main - entry point
 *
 * Return: 0 if False, otherwise if otherwise.
 */
int main(void)
{
	char ALP;
	
		if (ALP = 'a', ALP <= 'z', ALP++)
			{
			putchar(ALP);
			}

	 return (0);
}
