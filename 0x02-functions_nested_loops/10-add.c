#include "main.h"
/**
 * add - a function that adds two integers and returns the
 * result
 * @int: an integer
 * Return: returns the result.
 */
int add(int, int);
int add(int x, int y)
{
	int result;
		result = x + y;
		return (result);

}
