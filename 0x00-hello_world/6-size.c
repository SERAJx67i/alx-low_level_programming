#include <stdio.h>
/**
 * main - entry point
 *
 * Return always 0 when success  
 */
	int main(void)
{	
	sizeof(char);
	sizeof(int);
	sizeof(long int);
	sizeof(long long int);
	sizeof(float);

	return (0);
}
